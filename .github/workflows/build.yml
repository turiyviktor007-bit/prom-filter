name: Build EXE
on:
  push:
    branches: [ main ]
  workflow_dispatch:
jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install pyinstaller requests openpyxl
      - run: pyinstaller --onefile --windowed --name ProfetchNewProducts --hidden-import xml.etree.ElementTree --hidden-import openpyxl profetch_new_products.py
      - uses: actions/upload-artifact@v4
        with:
          name: ProfetchNewProducts
          path: dist/ProfetchNewProducts.exe
          retention-days: 30
